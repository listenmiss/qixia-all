<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间: 对应接口的包名+接口名 -->
<mapper namespace="cn.com.catcher.vision.gov.qixia.dao.SyncTriggerDao">
	<resultMap id="BaseResultMap" type="SyncTrigger">
		<result column="id" property="id" jdbcType="INTEGER" />
		<result column="sync_name" property="sync_name" jdbcType="VARCHAR" />
		<result column="sync_key" property="sync_key" jdbcType="VARCHAR" />
		
		

	</resultMap>

 	<insert id="insertSyncTrigger" parameterType="SyncTrigger">
		insert into sync_trigger
		<trim prefix="(" suffix=")" suffixOverrides=",">
			id,
			sync_name,
			sync_key
			
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			#{id},
			#{sync_name},
			#{sync_key}
		</trim>
	</insert> 


	<!-- 通过Id查找 -->
	<select id="findSyncTriggerById" parameterType="int" resultMap="BaseResultMap">
		select * from sync_trigger where id =#{id}
	</select>

	<select id="findAllSyncTrigger"  resultMap="BaseResultMap">
		select * from sync_trigger 
	</select>
	
	
	  <delete id="deleteSyncTriggerById" parameterType="int">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
       delete from sync_trigger
       where id = #{id}
  </delete>
</mapper>